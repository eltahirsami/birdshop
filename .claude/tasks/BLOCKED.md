# Blocked

- **WhatsApp daily report** — `sendDailyReport()` is implemented but the target number (`55951951@c.us`) is hardcoded and may be a placeholder. Needs real customer WhatsApp number before wiring up the cron. Also requires Puppeteer/Chromium to be available on the deployment machine (not always the case in POS environments).
